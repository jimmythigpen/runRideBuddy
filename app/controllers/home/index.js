import Ember from 'ember';

export default Ember.Controller.extend({

  isEditing: true,

//   distance: function(){
//     this.get('model').forEach(function(activity){
//       console.log(activity.distance);
//       return activity.distance;
//     });
//   }.property('model')
});
