<form class='submit-container' {{action 'authenticate' on='submit'}}>

<div class="submit-label">Run & Ride</div>

<div>{{input placeholder="email" class="submit-input" value=identification type='email'}}</div>
<div>{{input placeholder="password" class="submit-input" value=password type='password'}}</div>

<button class="submit-button" type="submit">LOGIN</button>

<div class="submit-link">{{#link-to 'register'}}need an account?{{/link-to}}</div>


</form>

{{outlet}}
